// Exercises 

let first_name = 'Ben';
let last_name = 'Groneman';

let full_name = `${first_name} ${last_name}`

console.log(full_name);


const num = 4936;

console.log(`${num % 10} is in the 1th place`);
console.log(`${num % 100} is in the 10th place`);
console.log(`${num % 1000} is in the 100th place`);

// Explicit coercion
console.log(Number('5') + 10);

